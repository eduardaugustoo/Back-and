// Objects (Objetos)
//Um objeto agrupa várias informações dentro de uma mesma variável, usando chave e valor.

let pessoa = {
    nome: "João",
    idade: 16,
    cidade: "São paulo"
};

console.log(pessoa.nome);
console.log(pessoa.idade);

//-cruie um objeto chamada 'carro' com as propiedades 'marcas', 'modelo' e 'ano'
//exiba cada propiedade no console.

let carro = {
    marca:"ford",
    modelo:"Mustang",
    ano:2020,
    cor:"preto",
};
console.log(carro.ano);

